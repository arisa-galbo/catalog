<h1>ブランド登録ページ</h1>

<%= form_with model: @brand do |form| %>
  <div>
    <%= form.label :name, "ブランド名" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "登録" %>
  </div>
<% end %>
