<h1>ブランド更新ページ</h1>

<%= form_with model: @brand do |form| %>
  <div>
    <%= form.label :name, "ブランド名" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "更新" %>
  </div>
<% end %>
