<h1>タグ一覧</h1>

<%= link_to "新しいタグを作成", new_admin_tag_path, class: "btn btn-primary" %>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, admin_tag_path(tag) %>
    </li>
  <% end %>
</ul>
