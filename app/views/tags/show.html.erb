<h1><%= @tag.name %></h1>

<h2>登録商品</h2>
<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, product_path(product) %></li>
  <% end %>
</ul>

<%= link_to "タグ一覧へ戻る", tags_path, class: "btn btn-secondary" %>