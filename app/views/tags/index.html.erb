<h1>タグ一覧</h1>

<% if authenticated? %>
<%= link_to "新しいタグを作成", new_tag_path, class: "btn btn-primary" %>
<% end %>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
    </li>
  <% end %>
</ul>
